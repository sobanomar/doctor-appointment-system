import React from "react";
import { Text, View } from "react-native";

const TestComponent = () => {
  return <Text className="text-red-600">TestComponent </Text>;
};

export default TestComponent;
